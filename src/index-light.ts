/*
 * Entity Progress Card, "-light" bundle: every card, badge and feature, no
 * visual editor - META.types.*.editor is then undefined (meta.ts, under
 * __EPB_LIGHT_BUILD__) and Home Assistant offers its own YAML editor instead.
 */

import { bootstrap } from './bootstrap.js';

bootstrap(null);
